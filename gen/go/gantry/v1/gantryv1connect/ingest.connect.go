// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gantry/v1/ingest.proto

package gantryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Sammyjroberts/gantry/gen/go/gantry/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// IngestServiceName is the fully-qualified name of the IngestService service.
	IngestServiceName = "gantry.v1.IngestService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// IngestServicePublishBatchProcedure is the fully-qualified name of the IngestService's
	// PublishBatch RPC.
	IngestServicePublishBatchProcedure = "/gantry.v1.IngestService/PublishBatch"
	// IngestServiceRegisterChannelsProcedure is the fully-qualified name of the IngestService's
	// RegisterChannels RPC.
	IngestServiceRegisterChannelsProcedure = "/gantry.v1.IngestService/RegisterChannels"
)

// IngestServiceClient is a client for the gantry.v1.IngestService service.
type IngestServiceClient interface {
	// Publish one batch of frames. Ack means durably accepted (written to the
	// stream), not merely received.
	PublishBatch(context.Context, *connect.Request[v1.PublishBatchRequest]) (*connect.Response[v1.PublishBatchResponse], error)
	// Register or update channel metadata for a device.
	RegisterChannels(context.Context, *connect.Request[v1.RegisterChannelsRequest]) (*connect.Response[v1.RegisterChannelsResponse], error)
}

// NewIngestServiceClient constructs a client for the gantry.v1.IngestService service. By default,
// it uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and
// sends uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC()
// or connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewIngestServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) IngestServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	ingestServiceMethods := v1.File_gantry_v1_ingest_proto.Services().ByName("IngestService").Methods()
	return &ingestServiceClient{
		publishBatch: connect.NewClient[v1.PublishBatchRequest, v1.PublishBatchResponse](
			httpClient,
			baseURL+IngestServicePublishBatchProcedure,
			connect.WithSchema(ingestServiceMethods.ByName("PublishBatch")),
			connect.WithClientOptions(opts...),
		),
		registerChannels: connect.NewClient[v1.RegisterChannelsRequest, v1.RegisterChannelsResponse](
			httpClient,
			baseURL+IngestServiceRegisterChannelsProcedure,
			connect.WithSchema(ingestServiceMethods.ByName("RegisterChannels")),
			connect.WithClientOptions(opts...),
		),
	}
}

// ingestServiceClient implements IngestServiceClient.
type ingestServiceClient struct {
	publishBatch     *connect.Client[v1.PublishBatchRequest, v1.PublishBatchResponse]
	registerChannels *connect.Client[v1.RegisterChannelsRequest, v1.RegisterChannelsResponse]
}

// PublishBatch calls gantry.v1.IngestService.PublishBatch.
func (c *ingestServiceClient) PublishBatch(ctx context.Context, req *connect.Request[v1.PublishBatchRequest]) (*connect.Response[v1.PublishBatchResponse], error) {
	return c.publishBatch.CallUnary(ctx, req)
}

// RegisterChannels calls gantry.v1.IngestService.RegisterChannels.
func (c *ingestServiceClient) RegisterChannels(ctx context.Context, req *connect.Request[v1.RegisterChannelsRequest]) (*connect.Response[v1.RegisterChannelsResponse], error) {
	return c.registerChannels.CallUnary(ctx, req)
}

// IngestServiceHandler is an implementation of the gantry.v1.IngestService service.
type IngestServiceHandler interface {
	// Publish one batch of frames. Ack means durably accepted (written to the
	// stream), not merely received.
	PublishBatch(context.Context, *connect.Request[v1.PublishBatchRequest]) (*connect.Response[v1.PublishBatchResponse], error)
	// Register or update channel metadata for a device.
	RegisterChannels(context.Context, *connect.Request[v1.RegisterChannelsRequest]) (*connect.Response[v1.RegisterChannelsResponse], error)
}

// NewIngestServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewIngestServiceHandler(svc IngestServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	ingestServiceMethods := v1.File_gantry_v1_ingest_proto.Services().ByName("IngestService").Methods()
	ingestServicePublishBatchHandler := connect.NewUnaryHandler(
		IngestServicePublishBatchProcedure,
		svc.PublishBatch,
		connect.WithSchema(ingestServiceMethods.ByName("PublishBatch")),
		connect.WithHandlerOptions(opts...),
	)
	ingestServiceRegisterChannelsHandler := connect.NewUnaryHandler(
		IngestServiceRegisterChannelsProcedure,
		svc.RegisterChannels,
		connect.WithSchema(ingestServiceMethods.ByName("RegisterChannels")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gantry.v1.IngestService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case IngestServicePublishBatchProcedure:
			ingestServicePublishBatchHandler.ServeHTTP(w, r)
		case IngestServiceRegisterChannelsProcedure:
			ingestServiceRegisterChannelsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedIngestServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedIngestServiceHandler struct{}

func (UnimplementedIngestServiceHandler) PublishBatch(context.Context, *connect.Request[v1.PublishBatchRequest]) (*connect.Response[v1.PublishBatchResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gantry.v1.IngestService.PublishBatch is not implemented"))
}

func (UnimplementedIngestServiceHandler) RegisterChannels(context.Context, *connect.Request[v1.RegisterChannelsRequest]) (*connect.Response[v1.RegisterChannelsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gantry.v1.IngestService.RegisterChannels is not implemented"))
}
