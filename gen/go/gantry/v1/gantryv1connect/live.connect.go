// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gantry/v1/live.proto

package gantryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Sammyjroberts/gantry/gen/go/gantry/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// LiveServiceName is the fully-qualified name of the LiveService service.
	LiveServiceName = "gantry.v1.LiveService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// LiveServiceSubscribeProcedure is the fully-qualified name of the LiveService's Subscribe RPC.
	LiveServiceSubscribeProcedure = "/gantry.v1.LiveService/Subscribe"
	// LiveServiceListChannelsProcedure is the fully-qualified name of the LiveService's ListChannels
	// RPC.
	LiveServiceListChannelsProcedure = "/gantry.v1.LiveService/ListChannels"
)

// LiveServiceClient is a client for the gantry.v1.LiveService service.
type LiveServiceClient interface {
	// Server-streaming live feed of frames for selected channels.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error)
	// Enumerate known devices and channels.
	ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error)
}

// NewLiveServiceClient constructs a client for the gantry.v1.LiveService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewLiveServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) LiveServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	liveServiceMethods := v1.File_gantry_v1_live_proto.Services().ByName("LiveService").Methods()
	return &liveServiceClient{
		subscribe: connect.NewClient[v1.SubscribeRequest, v1.SubscribeResponse](
			httpClient,
			baseURL+LiveServiceSubscribeProcedure,
			connect.WithSchema(liveServiceMethods.ByName("Subscribe")),
			connect.WithClientOptions(opts...),
		),
		listChannels: connect.NewClient[v1.ListChannelsRequest, v1.ListChannelsResponse](
			httpClient,
			baseURL+LiveServiceListChannelsProcedure,
			connect.WithSchema(liveServiceMethods.ByName("ListChannels")),
			connect.WithClientOptions(opts...),
		),
	}
}

// liveServiceClient implements LiveServiceClient.
type liveServiceClient struct {
	subscribe    *connect.Client[v1.SubscribeRequest, v1.SubscribeResponse]
	listChannels *connect.Client[v1.ListChannelsRequest, v1.ListChannelsResponse]
}

// Subscribe calls gantry.v1.LiveService.Subscribe.
func (c *liveServiceClient) Subscribe(ctx context.Context, req *connect.Request[v1.SubscribeRequest]) (*connect.ServerStreamForClient[v1.SubscribeResponse], error) {
	return c.subscribe.CallServerStream(ctx, req)
}

// ListChannels calls gantry.v1.LiveService.ListChannels.
func (c *liveServiceClient) ListChannels(ctx context.Context, req *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error) {
	return c.listChannels.CallUnary(ctx, req)
}

// LiveServiceHandler is an implementation of the gantry.v1.LiveService service.
type LiveServiceHandler interface {
	// Server-streaming live feed of frames for selected channels.
	Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error
	// Enumerate known devices and channels.
	ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error)
}

// NewLiveServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewLiveServiceHandler(svc LiveServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	liveServiceMethods := v1.File_gantry_v1_live_proto.Services().ByName("LiveService").Methods()
	liveServiceSubscribeHandler := connect.NewServerStreamHandler(
		LiveServiceSubscribeProcedure,
		svc.Subscribe,
		connect.WithSchema(liveServiceMethods.ByName("Subscribe")),
		connect.WithHandlerOptions(opts...),
	)
	liveServiceListChannelsHandler := connect.NewUnaryHandler(
		LiveServiceListChannelsProcedure,
		svc.ListChannels,
		connect.WithSchema(liveServiceMethods.ByName("ListChannels")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gantry.v1.LiveService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case LiveServiceSubscribeProcedure:
			liveServiceSubscribeHandler.ServeHTTP(w, r)
		case LiveServiceListChannelsProcedure:
			liveServiceListChannelsHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedLiveServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedLiveServiceHandler struct{}

func (UnimplementedLiveServiceHandler) Subscribe(context.Context, *connect.Request[v1.SubscribeRequest], *connect.ServerStream[v1.SubscribeResponse]) error {
	return connect.NewError(connect.CodeUnimplemented, errors.New("gantry.v1.LiveService.Subscribe is not implemented"))
}

func (UnimplementedLiveServiceHandler) ListChannels(context.Context, *connect.Request[v1.ListChannelsRequest]) (*connect.Response[v1.ListChannelsResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gantry.v1.LiveService.ListChannels is not implemented"))
}
