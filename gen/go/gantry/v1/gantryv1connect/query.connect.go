// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: gantry/v1/query.proto

package gantryv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/Sammyjroberts/gantry/gen/go/gantry/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// QueryServiceName is the fully-qualified name of the QueryService service.
	QueryServiceName = "gantry.v1.QueryService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// QueryServiceQueryRangeProcedure is the fully-qualified name of the QueryService's QueryRange RPC.
	QueryServiceQueryRangeProcedure = "/gantry.v1.QueryService/QueryRange"
)

// QueryServiceClient is a client for the gantry.v1.QueryService service.
type QueryServiceClient interface {
	// Fetch channels over [start_ns, end_ns]. Channels with more samples than
	// max_points_per_channel come back downsampled into min/max/mean buckets
	// (envelope-preserving); smaller results come back raw. Zooming in and
	// re-querying yields progressively higher resolution.
	QueryRange(context.Context, *connect.Request[v1.QueryRangeRequest]) (*connect.Response[v1.QueryRangeResponse], error)
}

// NewQueryServiceClient constructs a client for the gantry.v1.QueryService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewQueryServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) QueryServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	queryServiceMethods := v1.File_gantry_v1_query_proto.Services().ByName("QueryService").Methods()
	return &queryServiceClient{
		queryRange: connect.NewClient[v1.QueryRangeRequest, v1.QueryRangeResponse](
			httpClient,
			baseURL+QueryServiceQueryRangeProcedure,
			connect.WithSchema(queryServiceMethods.ByName("QueryRange")),
			connect.WithClientOptions(opts...),
		),
	}
}

// queryServiceClient implements QueryServiceClient.
type queryServiceClient struct {
	queryRange *connect.Client[v1.QueryRangeRequest, v1.QueryRangeResponse]
}

// QueryRange calls gantry.v1.QueryService.QueryRange.
func (c *queryServiceClient) QueryRange(ctx context.Context, req *connect.Request[v1.QueryRangeRequest]) (*connect.Response[v1.QueryRangeResponse], error) {
	return c.queryRange.CallUnary(ctx, req)
}

// QueryServiceHandler is an implementation of the gantry.v1.QueryService service.
type QueryServiceHandler interface {
	// Fetch channels over [start_ns, end_ns]. Channels with more samples than
	// max_points_per_channel come back downsampled into min/max/mean buckets
	// (envelope-preserving); smaller results come back raw. Zooming in and
	// re-querying yields progressively higher resolution.
	QueryRange(context.Context, *connect.Request[v1.QueryRangeRequest]) (*connect.Response[v1.QueryRangeResponse], error)
}

// NewQueryServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewQueryServiceHandler(svc QueryServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	queryServiceMethods := v1.File_gantry_v1_query_proto.Services().ByName("QueryService").Methods()
	queryServiceQueryRangeHandler := connect.NewUnaryHandler(
		QueryServiceQueryRangeProcedure,
		svc.QueryRange,
		connect.WithSchema(queryServiceMethods.ByName("QueryRange")),
		connect.WithHandlerOptions(opts...),
	)
	return "/gantry.v1.QueryService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case QueryServiceQueryRangeProcedure:
			queryServiceQueryRangeHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedQueryServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedQueryServiceHandler struct{}

func (UnimplementedQueryServiceHandler) QueryRange(context.Context, *connect.Request[v1.QueryRangeRequest]) (*connect.Response[v1.QueryRangeResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("gantry.v1.QueryService.QueryRange is not implemented"))
}
