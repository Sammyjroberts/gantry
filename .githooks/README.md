# .githooks

Repo-tracked Git hooks. `commit-msg` enforces Conventional Commits (see `.claude/skills/git/SKILL.md`).

Enable: `git config core.hooksPath .githooks` (run once per clone).

`just setup` does this for you.
